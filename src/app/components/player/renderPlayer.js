export function renderPlayer() {
  return `
    <section class="cantico-mini-player" aria-label="Reproductor">
      <div>
        <strong>Reproductor</strong>
        <span>Selecciona un himno para escuchar.</span>
      </div>
      <button type="button" disabled>▶ Escuchar</button>
    </section>
  `;
}
