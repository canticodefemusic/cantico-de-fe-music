
import { renderHeader } from "/components/header.js";import { renderFooter } from "/components/footer.js";import { renderPlayer,setQueue,nextSong,prevSong,togglePlay } from "/components/player.js";import { renderHome } from "/pages/home.js";import { renderHymns,renderHymnDetail } from "/pages/hymns.js";import { renderPlaylists } from "/pages/playlists.js";import { renderAlbums } from "/pages/albums.js";import { renderVideos } from "/pages/videos.js";import { renderDevotionals } from "/pages/devotionals.js";import { renderAdmin } from "/pages/admin.js";
export const App={data:{hymns:[],devotionals:[],playlists:[],albums:[],settings:{}},async loadData(){const [hymns,devotionals,playlists,albums,settings]=await Promise.all([fetch("/assets/data/hymns.json").then(r=>r.json()),fetch("/assets/data/devotionals.json").then(r=>r.json()),fetch("/assets/data/playlists.json").then(r=>r.json()),fetch("/assets/data/albums.json").then(r=>r.json()),fetch("/assets/data/settings.json").then(r=>r.json())]);this.data={hymns,devotionals,playlists,albums,settings}},route(){const params=new URLSearchParams(location.search),page=params.get("page")||"home",id=params.get("id");const routes={home:()=>renderHome(this.data),hymns:()=>renderHymns(this.data),hymn:()=>renderHymnDetail(this.data,id),playlists:()=>renderPlaylists(this.data),albums:()=>renderAlbums(this.data),videos:()=>renderVideos(this.data),devotionals:()=>renderDevotionals(this.data),admin:()=>renderAdmin(this.data)};return (routes[page]||routes.home)()},setSchema(){const s={"@context":"https://schema.org","@type":"MusicGroup","name":this.data.settings.siteName,"url":this.data.settings.siteUrl,"description":"Himnos cristianos originales, letras, videos y devocionales."};document.querySelector("#schemaData").textContent=JSON.stringify(s)},async start(){await this.loadData();this.setSchema();document.querySelector("#app").innerHTML=`${renderHeader(this.data.settings)}<main id="main"></main>${renderFooter(this.data.settings)}${renderPlayer()}`;document.querySelector("#main").innerHTML=this.route();this.bindGlobalEvents()},bindGlobalEvents(){document.querySelector(".mobile-toggle")?.addEventListener("click",()=>document.querySelector(".menu")?.classList.toggle("open"));document.querySelector("#themeToggle")?.addEventListener("click",()=>{document.body.classList.toggle("dark");localStorage.theme=document.body.classList.contains("dark")?"dark":"light"});if(localStorage.theme==="dark")document.body.classList.add("dark");document.addEventListener("click",event=>{const play=event.target.closest("[data-play]");if(play){const id=play.dataset.play,start=this.data.hymns.findIndex(h=>h.id===id);setQueue(this.data.hymns,start<0?0:start)}if(event.target.closest("[data-next]"))nextSong();if(event.target.closest("[data-prev]"))prevSong();if(event.target.closest("[data-toggle]"))togglePlay();const playlist=event.target.closest("[data-playlist]");if(playlist){const list=this.data.playlists.find(p=>p.id===playlist.dataset.playlist);const hymns=list.items.map(id=>this.data.hymns.find(h=>h.id===id)).filter(Boolean);setQueue(hymns,0)}const album=event.target.closest("[data-album]");if(album){const list=this.data.albums.find(a=>a.id===album.dataset.album);const hymns=list.items.map(id=>this.data.hymns.find(h=>h.id===id)).filter(Boolean);setQueue(hymns,0)}})}};window.App=App;App.start();
