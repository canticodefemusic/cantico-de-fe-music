export function renderPlayerPage(data){return `<section class="section"><div class="container"><div class="section-head"><p class="eyebrow">V7.0</p><h2>Reproductor avanzado</h2><p>Cola, progreso, volumen, repetir, aleatorio y soporte para MP3 real.</p></div><div class="list">${data.hymns.map(h=>`<article class="hymn-row"><div class="thumb">▶</div><div><h3 style="font-size:22px;margin:0">${h.title}</h3><p class="muted">${h.audioUrl?"MP3 conectado":"Demo sin MP3"}</p></div><button class="btn primary" data-play="${h.id}">▶ Cargar</button></article>`).join("")}</div></div></section>`}
