export function renderLyrics(data){const all=[...data.hymns,...(data.localDrafts||[])];return `<section class="section"><div class="container"><div class="section-head"><h2>Letras</h2><p>Todas las letras organizadas para leer y compartir.</p></div><div class="list">${all.map(h=>`<article class="panel"><p class="eyebrow">${h.reference}</p><h3 style="font-size:32px">${h.title}</h3>${h.lyrics.map(l=>l?`<p>${l}</p>`:"<br>").join("")}</article>`).join("")}</div></div></section>`}
