
import { validateMediaLibrary } from "/assets/js/services/mediaService.js";
function mediaCard(item){const icon=item.type==="audio"?"🎧":item.type==="video"?"🎬":"🖼";const cls=item.type==="audio"?"audio":item.type==="video"?"video-art":"cover-art";return `<article class="card"><div class="media-thumb ${cls}">${icon}</div><span class="badge">${item.type}</span><h3>${item.title}</h3><p class="muted">Estado: ${item.status}</p><span class="media-path">${item.path}</span></article>`}
export function renderMedia(data){const report=validateMediaLibrary(data.hymns);return `<section class="library-hero"><div class="container"><p class="eyebrow">V7.0</p><h1 style="font-size:clamp(50px,6vw,78px)">Media Library</h1><p class="lead">Portadas, MP3 y videos organizados.</p></div></section><section class="section"><div class="container"><div class="media-grid">${data.media.map(mediaCard).join("")}</div><h3 style="font-size:34px;margin:34px 0 16px">Rutas sugeridas</h3><div class="list">${report.map(r=>`<article class="panel"><h3 style="font-size:26px">${r.title}</h3><span class="media-path">${r.cover}</span><span class="media-path">${r.audio}</span><span class="media-path">${r.video}</span></article>`).join("")}</div></div></section>`}
