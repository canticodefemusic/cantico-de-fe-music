
import { setPageMeta } from "/assets/js/utils/seo.js";
import { searchHymns } from "/assets/js/services/searchService.js";
import { getById, getRelatedHymns } from "/assets/js/services/contentService.js";
import { hymnRow } from "/components/cards.js";
export function renderHymns(data){const themes=[...new Set(data.hymns.map(h=>h.theme))];setTimeout(()=>{const s=document.querySelector("#hymnSearch"),t=document.querySelector("#themeFilter"),l=document.querySelector("#hymnList");function draw(){const items=searchHymns(data.hymns,s.value,t.value);l.innerHTML=items.map(hymnRow).join("")||`<div class="panel">No se encontraron himnos.</div>`}s.addEventListener("input",draw);t.addEventListener("input",draw);draw()});return `<section class="library-hero"><div class="container"><p class="eyebrow">Biblioteca V6.3</p><h1 style="font-size:clamp(50px,6vw,78px)">Himnos</h1><p class="lead">Biblioteca con validación admin y reproductor avanzado.</p><div class="library-stats"><div class="stat"><strong>${data.hymns.length}</strong><p>Himnos</p></div><div class="stat"><strong>${themes.length}</strong><p>Temas</p></div><div class="stat"><strong>${data.playlists.length}</strong><p>Playlists</p></div><div class="stat"><strong>Admin</strong><p>V6.3</p></div></div></div></section><section class="section"><div class="container music-shell"><aside class="music-side panel"><h3>Explorar</h3><a href="/?page=admin">Admin <span>›</span></a><a href="/?page=player">Player <span>›</span></a><a href="/?page=lyrics">Letras <span>›</span></a></aside><div><div class="filters"><input id="hymnSearch" class="input" placeholder="Buscar himno, palabra de letra o referencia..."><select id="themeFilter"><option value="">Todos los temas</option>${themes.map(t=>`<option>${t}</option>`).join("")}</select></div><div id="hymnList" class="list">${data.hymns.map(hymnRow).join("")}</div></div></div></section>`}
export function renderHymnDetail(data,id){const h=getById(data.hymns,id)||data.hymns[0],related=getRelatedHymns(data.hymns,h,3);setTimeout(()=>setPageMeta(h.seo?.title||h.title,h.seo?.description||h.description));return `<section class="section"><div class="container"><a class="muted" href="/?page=hymns">← Volver a Himnos</a><div class="section-head" style="margin-top:20px"><h2>${h.title}</h2><p>${h.subtitle}</p></div><div class="featured"><div class="panel"><p class="eyebrow">Audio V6.3</p><div class="player"><button class="play" data-play="${h.id}">▶</button><div class="progress"><span></span></div><strong>${h.duration}</strong></div><p><strong>Referencia:</strong> ${h.reference}</p><p><strong>Tema:</strong> ${h.theme}</p><p><strong>Álbum:</strong> ${h.album}</p><p><strong>Autor:</strong> ${h.author}</p><div class="seo-box"><div class="seo-title">${h.seo?.title||h.title}</div><div class="seo-url">canticodefemusic.com/?page=hymn&id=${h.id}</div><div class="seo-desc">${h.seo?.description||h.description}</div></div></div><div class="panel"><p class="eyebrow">Letra</p>${h.lyrics.map(l=>l?`<p>${l}</p>`:"<br>").join("")}</div></div><section class="section"><h3 style="font-size:34px;margin-bottom:16px">Relacionados</h3><div class="list">${related.map(hymnRow).join("")||'<p class="muted">No hay relacionados todavía.</p>'}</div></section></div></section>`}
